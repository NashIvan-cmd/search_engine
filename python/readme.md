python3 -m venv venv
source venv/bin/activate

pip install pandas

Normalizing the Data
Every data must be uniformed

Importance of Indexing
Indexing is a powerful way to make the Search Engine faster by 10x

Corpus
Jaccard